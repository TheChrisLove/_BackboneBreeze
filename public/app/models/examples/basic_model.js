/* global define, console */
define([
  'underscore',
  'backbone',
], function (_, Backbone) {
    "use strict";

    var Model = Backbone.Model.extend({

        defaults: {},

    });

    return Model;
});